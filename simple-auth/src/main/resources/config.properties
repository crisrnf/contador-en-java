# Database configuration - update these values before running
# Example:
# db.url=jdbc:mysql://localhost:3306/simple_auth?serverTimezone=UTC
# db.user=root
# db.password=yourpassword

# Defaults used by the app if these are not provided
